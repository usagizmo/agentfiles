---
name: consult
description: >-
  設計・方針のセカンドオピニオン。複数案が存在し得る設計判断をしたとき、中規模以上の見込みで着手するとき、
  中規模以上の diff を書き終えたときに発動する（ユーザーの明示は不要）。
  skip できるのは選択肢が実質 1 つの自明な変更だけ。
---

# 設計判断の突き合わせ

**判断主体は実行中のエージェント**。アドバイザー（別モデル）は検証者であって命令ではない。判定基準と規模の定義は `~/.agents/AGENTS.md`。技術的収束はアドバイザーへ、製品判断はユーザーへ。

## モード

| モード     | いつ                           | 出すもの                                                              | 終わり方                                |
| ---------- | ------------------------------ | --------------------------------------------------------------------- | --------------------------------------- |
| 軽量       | 修正方針・短い技術的疑問       | 問い / 案 / 触るパス / 懸念（3〜6 行）                                | 統合して 5〜10 行で提示                 |
| 深い・事前 | 中規模以上の着手前             | ゴール / 変えるファイル / 主要判断と理由 / 懸念                       | **ユーザーの GO** まで実装しない        |
| 深い・事後 | 中規模以上の diff を書き終えた | ゼロベースの理想形と現状の差（責務・API・データフロー・SSOT・エッジ） | **指摘が尽きるまで回す**。GO は要らない |

## 手順

1. 自分の判断を書く（モードの「出すもの」）
2. プロンプトを `mktemp` のファイルへ書く（下記。書き方は `references/advisors.md`）
3. `<skills root>/consult/scripts/advisors.sh start <prompt-file>` → `collect <run-dir>`
4. 統合する。論点ごとに 一致 / 取り込み / 不採用（理由）/ ユーザー判断が要る点。複数アドバイザーなら出典タグを付す。`start` が失敗して run dir が無いときは、自分の判断だけを出して 1 巡で終える
5. 事後は修正して 2 へ戻る。承認済みの範囲内の技術修正は自律で直し、製品判断が変わるときだけ聞く

事前の GO ゲートに載せるのは 仕様 / 変更対象 / 主要判断と理由 / スコープ外 / 検証 / アドバイザーとの一致・取り込み・不採用。Plan Mode がある harness はそれを使う。

指摘を実測で不採用にするときは、測った対象（ディレクトリ・生成物・時点）が指摘の対象と一致することを確かめる。ずれているなら測り直す。

事後の打ち切りは、指摘なし、または残指摘をすべて棄却できたとき。**「指摘なし」で打ち切れるのは、回収ヘッダが全員 `rc=0` の巡だけ**（欠落した目は沈黙の証拠にならない）。堂々巡りならユーザーに相談。

## プロンプト

```markdown
あなたは設計判断のレビュアーです。コードは変更せず分析と提案だけ返してください。

## 必読

- ~/.agents/AGENTS.md（設計原則・ボーイスカウトルール）

## 背景・要件

{...}

## レビュー対象

{事前: パス + 注目点。全文は貼らない}
{事後: git status --short / git diff / git diff --cached / untracked の中身}

## 私の判断

{...}

## 観点

- 設計原則からの逸脱（ゼロベース一致・根本解決・互換 shim / deprecated / dead code の残存）
- SSOT 複製（test が production 形を手コピー・同一 helper の二重）、shape / 型 / mock / 生成物の drift
- 経緯コメント（「以前は〜だった」・Issue / PR 番号）
- バグ・エッジケース・軸の混在・より良い代替・隠れたトレードオフ

## 出力

重要度順「ファイル:行 / 問題 / 推奨修正」。なければ「指摘なし」のみ。nitpick 不要。
末尾: このまま進めてよい / 修正推奨 / 再考推奨（一言）。
```
