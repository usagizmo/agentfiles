# marker の upsert

記録のコメントを 1 つに保つ手順。**書き先と欄の中身は呼び出し元が持つ。**

- 書き先のコメントを全部取る。**兄弟 Issue は見ない**（成員側の既存は探さない・消さない）
- 単独行の marker を数える（コメント件数ではなく block 数）
- 0 件なら 1 つ作る。1 件ならそのコメントを書き換える。2 件以上なら書かずに**記録不能**
- 更新は上書きにし**ない**。既存の欄を差し替える
- upsert が成功したことを確かめる。失敗したら聞かずに「記録不能」として報告して終える
