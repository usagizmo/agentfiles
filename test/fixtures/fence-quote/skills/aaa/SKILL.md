---
name: aaa
description: fixture
---

# aaa

手順は `references/host.md`。

```sh
# 実例。`commit` を名指しし、`nope.md` を挙げ、`references/host.md` の「無い見出し」を指す
git commit
```

> 引用。`commit` を名指しし、`references/host.md` の「無い引用見出し」を指す。

外に在る違反は `missing/gone.md`。
